/**
 *   This project demonstrates the uNabto framework running the
 *   uNabto SDK demo on a Raspberry Pi (Linux).
 *
 *   SunPi control center:
 *   1) Install CMake, "sudo apt-get install cmake"
 *   2) Run "cmake ." in the "raspberry_pi_sunpi" directory.
 *   3) Run "make" to make the executable.
 *   4) And run the newly made unabto_raspberrypi by e.g. "./unabto_raspberrypi -d [ yourName ].demo.nabto".
 *
 */